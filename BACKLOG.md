# Backlog

## Технический долг

- [ ] **Индикатор частичных ошибок в ChatHistoryDrawer**

- [ ] **Обработка `event: error` в SSE-стриме** (Responses API)  
       README: ошибка генерации → `event: error` с `sequence_number: 9999`.  
       **Приоритет: высокий.** Мы используем только Responses API —  
       `event: error` — реальный сценарий, не гипотетический.  
       Требует: обработки в `SendMessageUseCase` + новый `ChatStreamEvent.failed`.

- [ ] **Различение 502 от Master Router и от document_chat**  
       Master Router: 502 = агент недоступен.  
       document_chat: 502 = MinerU упал.  
       Сейчас всегда показываем «Не удалось обработать документ».  
       Требует: доработки бэкенда (`error.code`) или разбора тела.

- [ ] **Различение 400 от Master Router и от document_chat**  
       Master Router: 400 = невалидное тело или `contract_forms` не совпал.  
       document_chat: 400 = файл не обработан.  
       Сейчас всегда показываем «Файл ещё обрабатывается».  
       Требует: доработки бэкенда (`error.code`).

- [ ] **Показывать serverMessage в userMessage при 400 в /v1/responses**  
       Если агент не поддерживает Responses — сервер вернёт 400  
       с текстом «Агент X не поддерживает форму Responses API».  
       Сейчас пользователь видит «Некорректный запрос».  
       Требует: разбора `error.message` для конкретных 400.

- [ ] **Уточнить у бэкендера: будет ли `actual_bytes`/`max_bytes` в 413**
      Сейчас nginx отсекает файлы до бэкенда, поэтому неизвестно.
      **После исправления nginx** — уточнить, планирует ли бэкенд
      присылать эти поля. Если да — клиент покажет точные цифры.
      Код уже готов к обоим вариантам (fallback есть).

- [ ] **Бэкенд: присылать actual_bytes / max_bytes в 413**  
       Сейчас `tooLargeFromServer` показывает общее сообщение — мы не знаем точных цифр.  
       Когда бэкенд начнёт присылать эти поля, клиент сможет показать «Файл слишком большой (32.9 МБ). Максимум — 25 МБ.»  
       Требует: доработки бэкенда + парсинга в `AttachmentRepository`.

- [ ] **Починить `MessageDto.sources` — это `List<String>`, не `List<Map>`**  
       README бэкенда: `sources` = `[filename]`. Сейчас парсится как  
       `List<Map<String, dynamic>>?` — упадёт с TypeError на непустом `sources`.  
       Приоритет: **высокий**. Починить при рефакторинге MessageDto.

- [ ] **Обработка `event: error` в SSE-стриме**  
       README: ошибка генерации → `event: error` с `sequence_number: 9999`.  
       Сейчас `SendMessageUseCase` игнорирует это событие — стрим просто закроется,  
       пользователь увидит пустой ответ без объяснения.  
       Требует: обработки в `SendMessageUseCase` + новый `ChatStreamEvent.failed`.

- [ ] **Расширить `AppConfig.allowedMimeTypes` (уточнить у техдира)**  
       README: «PDF/DOCX/изображение». MinerU умеет DOCX/PPTX/XLSX.  
       Уточнить, что реально включено на деплое, и добавить.

- [ ] **UUID-валидация `X-User-Id`**  
       README: невалидный UUID → 401. Сейчас `USER_ID` из .env — любая строка.  
       Требует: замены на AuthService при интеграции с Keycloak.

- [ ] **`GET /v1/files`, `GET /v1/files/{id}`, `DELETE /v1/files/{id}`**  
       В `AttachmentApi` пока только `uploadFile`.  
       Нужно для: удаления файла с сервера при `removeAttachment`,  
       отображения «моих файлов», отладки.

- [ ] **Различение `401` (не залогинен) и `401` (невалидный UUID)**  
       Сейчас общее сообщение «Не авторизован».

- [ ] **`error.param` от бэкенда → в `technicalDetails`**  
       README: `param` заполняется (`messages.0.role`). Полезно для отладки.

- [ ] **Роли `system`/`developer` в MessageDto**  
       Пока не приходят от бэкенда, но могут.

- [ ] **Явное различение «первое сообщение» vs «с историей» в `_buildInput`**  
       Сейчас работает случайно (мы всегда в чате с conversation_id).  
       При «регенерации» или «отправке без сохранения» — сломается.

## Фичи

- [ ] Кнопки агентов (ручной выбор)
- [ ] Фидбек (лайк/дизлайк)
- [ ] Источники (отображение)
- [ ] Авторизация с ролями
- [ ] Голосовой ввод
- [ ] Кэширование / оффлайн-режим
- [ ] Настройки

- [ ] **UI-индикатор долгой обработки файла**  
       MinerU может обрабатывать файл 30–120 секунд.  
       Показать: «Обработка документа... Это может занять минуту» + таймер.  
       Опционально: кнопка «Отменить».

## Идеи
